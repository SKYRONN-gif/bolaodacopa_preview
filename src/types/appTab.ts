/**
 * Define os nomes permitidos para a aba ativa da aplicação.
 *
 * É usado pelo estado local do React para indicar qual aba está selecionada
 * e, consequentemente, qual conteúdo deve aparecer na tela.
 */
export type AppTab = 'home' | 'matches' | 'ranking' | 'admin';